import Slider from '../Slider';

const Brands = () => {
    return (
        <section id="brand-section">
            <h2 className="section-title">
                <Slider />
            </h2>
        </section>
    );
};
export default Brands;
